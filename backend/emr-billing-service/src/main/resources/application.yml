server:
  port: 8107

spring:
  application:
    name: emr-billing-service
  datasource:
    url: ${EMR_BILLING_DB_URL:jdbc:mysql://localhost:3306/emr_billing?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${EMR_BILLING_DB_USERNAME:root}
    password: ${EMR_BILLING_DB_PASSWORD:root}
    driver-class-name: ${EMR_BILLING_DB_DRIVER:com.mysql.cj.jdbc.Driver}

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
