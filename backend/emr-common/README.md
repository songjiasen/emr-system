# emr-common

公共基础模块，不单独启动。

## 模块职责

- 统一返回结构、错误码、异常定义。
- 公共常量、角色编码、业务编号前缀。
- 通用工具类和跨服务复用的基础对象。
- `TrustedUserContext` 负责解析网关注入的 `X-User-Id`、`X-Username`、`X-Role-Code`、`X-User-Table`，供业务服务做角色和数据范围校验。

## 使用方式

其他后端服务通过 Maven 依赖引用本模块。公共能力必须保持稳定，避免在这里放具体业务逻辑。
