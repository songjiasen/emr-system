# 安心医疗系统功能与流程核对文档

> 核对日期：2026-06-23  
> 核对依据：`/Users/song/Downloads/需求分析文档.pdf`、`/Users/song/Downloads/概要设计文档.pdf`、当前项目代码、各微服务 README、已整理 API/数据库文档。

## 1. 核对结论

当前项目已经按照“微服务实现”的方向，把 PDF 中要求的核心模块基本拆成了认证、用户科室、就诊流程、病历、诊疗、工作流、费用、系统内容、AI、网关等服务。患者端和后台端也已经具备对应页面，能完成期末作业演示所需的主要链路。

整体结论是：

- 核心业务模块：基本实现。
- 患者端流程：预约、病历/处方/检查/费用查看、资讯留言、个人中心、AI 功能已覆盖。
- 后台端流程：预约、用户科室、病历、住院、模板归档、医嘱处方检查、工作流、费用、系统配置、AI 已覆盖。
- 主流程串联：可以串起来演示，但部分环节仍以“手动选择/填写业务编号、病历 ID、患者 ID、医生 ID”的方式衔接，没有做到所有步骤自动带入上下文。
- 和 PDF 原始设计的差异：PDF 偏单体后端和传统管理页面设计，当前项目改成了微服务 + 网关 + 两个前端应用，接口形态更 RESTful，但保留了 `/cl584734139` 统一前缀，便于兼容原有前端访问习惯。

## 2. 当前系统微服务划分

| 服务 | 当前职责 | 对应 PDF 功能 |
| --- | --- | --- |
| `emr-gateway` | 统一入口、Token 校验、角色访问控制、服务转发、操作审计 | 统一 API 入口、登录拦截、权限控制、系统日志 |
| `emr-auth-service` | 登录、注册、退出、Token 校验、改密、忘记密码 | 用户登录、患者注册、密码管理、Token 认证 |
| `emr-user-service` | 医生、患者、管理员、护士、主任、科室维护 | 用户管理、角色管理、科室管理、医生介绍 |
| `emr-visit-service` | 预约挂号、取消预约、分诊、入院、出院 | 预约挂号、分诊建档、入院登记、出院办理 |
| `emr-record-service` | 病历、附件示例、既往史、模板、归档申请、归档记录 | 病历管理、病历模板、病历归档 |
| `emr-clinical-service` | 医嘱、医嘱执行、处方、检查申请 | 医嘱管理、开方管理、检查项目 |
| `emr-workflow-service` | 审核任务、审核记录、医嘱/检查/归档回写 | 工作流、主任审核、归档审核 |
| `emr-billing-service` | 费用创建、查询、修改、删除、支付 | 费用管理、患者费用查看 |
| `emr-system-service` | 资讯、留言、轮播图、菜单、配置、系统日志 | 病历资讯、留言板、菜单、系统配置、系统日志 |
| `emr-ai-service` | OCR、智能荐药、处方审核、智能检索 | AI 智能模块 |
| `emr-common` | 公共响应、异常、角色常量、可信身份上下文 | 公共基础能力 |

## 3. PDF 功能覆盖矩阵

覆盖状态说明：

- 已实现：接口、数据表、页面或服务能力均已具备，可直接演示。
- 基本实现：主能力已具备，部分细节和 PDF 不完全一致，或需要手动串联。
- 部分实现：已有接口或后端能力，但前端入口、自动联动、权限细节或业务闭环仍需增强。
- 待补齐：当前项目中未形成完整能力。

| PDF 功能模块 | PDF 要求摘要 | 当前实现 | 覆盖状态 | 说明 |
| --- | --- | --- | --- | --- |
| 登录认证 | 登录、患者注册、退出、Token 认证、过期处理、改密、忘记密码 | `emr-auth-service` + 网关 Token 校验；前端已有独立登录/注册页和退出 | 已实现 | 忘记密码后端已具备，前端如需完全贴合 PDF，可再补独立入口 |
| 用户管理 | 管理员、医生、患者、角色维护，增删改查 | `emr-user-service` 的用户管理接口；后台端用户/医生/患者/科室管理 | 基本实现 | 角色以 `roleCode` 和菜单配置实现，没有单独做完整角色权限表维护页面 |
| 科室管理 | 科室列表、新增、编辑、删除、医生归属 | 科室接口和后台端管理页已实现，患者端可按科室看医生 | 已实现 | 与医生介绍、预约挂号均可关联 |
| 医生介绍 | 医生列表、详情、科室/职称/擅长展示 | 医生接口和患者端医生展示已实现 | 已实现 | 视觉样式已按参考图调整为安心医疗风格 |
| 预约挂号 | 患者预约、查看预约、取消预约、预约编号 | `emr-visit-service` 预约接口；患者端预约弹窗和预约列表 | 已实现 | 预约创建已修复成功后列表不刷新/无数据问题 |
| 分诊建档 | 护士分诊，形成就诊前置记录 | `emr-visit-service` 分诊接口；后台端有就诊流程入口 | 基本实现 | 和预约/病历之间可以通过患者、预约信息手动串联，自动流转还可增强 |
| 病历管理 | 医生录入、编辑、删除、查看病历，患者查看本人病历，病历编号、附件、既往史 | `emr-record-service`；后台病历管理、患者端就诊记录 | 基本实现 | 病历已补 `appointment_id`、`appointment_no` 方便串预约；附件偏演示口径 |
| 病历模板 | 模板增删改查、医生引用模板 | `emr-record-service` 模板接口；后台模板管理 | 已实现 | 删除为软删除，符合演示和安全要求 |
| 处方管理 | 医生开方、患者查看处方、处方明细 | `emr-clinical-service` 处方接口；前后台均有入口 | 已实现 | 当前以业务 CRUD 和患者查看为主，未纳入主任工作流审核 |
| 医嘱管理 | 医嘱创建、审核、执行、执行记录 | `emr-clinical-service` + `emr-workflow-service` | 已实现 | 医嘱审核通过后才能执行；工作流会回写医嘱审核状态 |
| 检查项目 | 检查申请、审核、结果维护、患者查看 | `emr-clinical-service` 检查申请 + 工作流回写 | 已实现 | 支持审核结果回写，检查结果录入后可完成 |
| 入院管理 | 入院登记、病房床位、出院办理 | `emr-visit-service` 入院/出院接口；后台住院相关页面 | 基本实现 | 床位占用校验已实现；和病历/费用自动生成链路可继续增强 |
| 费用管理 | 费用记录、支付、患者费用查看 | `emr-billing-service`；后台费用管理、患者端费用查看/支付 | 基本实现 | 支持业务类型和业务 ID 关联，但费用目前不会自动由处方/检查/入院生成 |
| 病历归档 | 归档申请、审核、归档记录、状态流转 | `emr-record-service` + `emr-workflow-service` | 已实现 | 归档审核通过会更新病历归档状态并生成归档记录 |
| 病历资讯 | 资讯发布、列表、详情、编辑、删除 | `emr-system-service`；前端内容管理和患者端资讯 | 基本实现 | 后端完整，前端管理能力已覆盖主要发布/展示，细粒度编辑入口可按需要继续补 |
| 留言板 | 患者留言、工作人员回复、删除 | `emr-system-service`；患者端/后台端均有入口 | 已实现 | 患者默认看自己的留言；工作人员可回复 |
| 系统管理 | 菜单、轮播图、配置、系统日志 | `emr-system-service` + 网关审计 | 已实现 | 写操作经网关记录审计日志 |
| AI 智能 | OCR、荐药、处方审核、智能检索 | `emr-ai-service`；前后台 AI 页面 | 基本实现 | 默认使用 mock provider，适合期末演示；接真实百度 AI 需配置外部 Key |
| 权限与安全 | Token、角色访问、患者只能看本人数据、密码加密、操作日志 | 网关 + 各服务可信身份头校验 + BCrypt + syslog | 基本实现 | 核心安全口径已具备；细粒度前端按钮权限可继续增强 |
| 性能/分页 | 分页查询、响应时间、文件大小限制 | 多数列表接口支持分页，前端有分页 | 基本实现 | 文件上传目前偏演示，未完整实现 100MB 上传存储治理 |

## 4. 主流程串联情况

PDF 中的核心医疗业务流程可以概括为：

```text
患者注册/登录
  -> 预约挂号
  -> 医生接诊并录入病历
  -> 开医嘱 / 开处方 / 开检查申请
  -> 主任审核医嘱或检查
  -> 护士执行医嘱 / 医生录入检查结果
  -> 费用生成与患者支付
  -> 入院登记 / 出院办理
  -> 病历归档申请
  -> 主任或管理员审核
  -> 归档完成
```

当前项目的实际串联情况如下。

### 4.1 患者注册登录到预约挂号

流程：

```text
患者注册 -> 登录获取 Token -> 网关校验 Token -> 患者端创建预约 -> 预约列表展示
```

当前状态：已串通。

说明：

- 注册、登录、退出、Token 校验、患者数据范围控制已经具备。
- 患者预约会落到 `yuyueguahao`，创建成功后可刷新列表查看。
- 医生、科室数据来自用户科室服务，预约可以选择医生和预约时间。

### 4.2 预约挂号到病历录入

流程：

```text
预约记录 -> 医生或后台查看预约 -> 创建病历 -> 病历保存 appointment_id / appointment_no
```

当前状态：可以串联，但前端自动带入还不够完整。

说明：

- 病历表已补充 `appointment_id`、`appointment_no` 字段，这是当前项目为串联预约和病历做的关键补充。
- 后台可以录入病历，也可以通过患者、医生、预约号等信息形成关联。
- 目前不是所有入口都做到“从某条预约点击接诊后自动打开病历弹窗并自动带入预约信息”，演示时需要在病历表单中选择或填写关联信息。

建议后续增强：

- 在预约列表增加“接诊/录入病历”按钮。
- 点击后打开新增病历弹窗，并自动带入患者、医生、科室、预约编号、预约时间。
- 病历创建成功后同步更新预约状态为已就诊。

### 4.3 病历到医嘱、处方、检查

流程：

```text
病历创建 -> 医生创建医嘱 / 处方 / 检查申请 -> 患者端可查看相关记录
```

当前状态：可以串联，部分上下文仍需手动选择。

说明：

- 医嘱、处方、检查申请均支持 `patientId`、`doctorId`、`medicalRecordId` 等业务字段。
- 医生通过网关创建诊疗数据时，服务会基于可信身份头限制医生只能操作自己的诊疗数据。
- 患者端可以查看自己的处方、检查、病历等信息。
- 当前前端更多是模块化管理页面，没有完全做成“从病历详情一键开医嘱/开处方/开检查”的强流程。

建议后续增强：

- 在病历详情页增加“开医嘱”“开处方”“开检查”按钮。
- 弹窗自动带入病历 ID、患者 ID、医生 ID，减少演示时手动填错。

### 4.4 医嘱审核与执行

流程：

```text
医生创建医嘱
  -> 主任或管理员创建/处理审核任务
  -> 工作流服务回写医嘱审核结果
  -> 护士执行已审核医嘱
  -> 生成执行记录并更新医嘱状态
```

当前状态：已串通。

说明：

- `emr-workflow-service` 支持 `medical_order` 类型审核任务。
- 审核通过或驳回时，会调用诊疗服务的医嘱审核回写接口。
- 诊疗服务限制医嘱必须审核通过后才能执行。
- 执行成功后会新增执行记录，并把医嘱状态更新为 `executed`。

注意：

- 当前没有单独拆出医生端、护士端、主任端三个独立应用，而是在后台端统一承载这些操作。
- 若老师要求按角色严格展示不同工作台，建议继续补角色化菜单和页面入口。

### 4.5 检查申请审核与结果维护

流程：

```text
医生提交检查申请
  -> 主任或管理员审核
  -> 工作流服务回写检查申请状态
  -> 医生或工作人员维护检查结果
  -> 患者查看检查记录
```

当前状态：已串通。

说明：

- `emr-workflow-service` 支持 `test_request` 类型审核任务。
- 审核结果会回写 `emr-clinical-service`。
- 检查申请可以维护结果，状态可进入完成态。

### 4.6 分诊、入院、出院

流程：

```text
护士分诊建档 -> 入院登记和床位分配 -> 出院办理 -> 入院记录更新为已出院
```

当前状态：基本串通。

说明：

- 分诊、入院、出院均已由 `emr-visit-service` 持久化。
- 入院登记有床位占用校验。
- 出院办理会同步更新对应入院记录为已出院，并拦截重复出院。

不足：

- 分诊和入院当前更像后台管理动作，还没有和预约、病历、费用完全自动联动。
- 如果希望完整模拟医院流程，可补“从预约/病历转入院”的快捷入口。

### 4.7 费用生成与支付

流程：

```text
后台创建费用 -> 患者端查看费用 -> 患者支付 -> 支付状态更新
```

当前状态：基本串通。

说明：

- 费用支持按患者、支付状态、业务类型、业务 ID 查询。
- 患者只能查看和支付自己的费用。
- 支付只允许从未支付流转为已支付。

不足：

- 当前费用需要后台手动创建。
- 处方、检查、住院不会自动生成费用记录。

建议后续增强：

- 处方创建成功后按药品明细生成费用。
- 检查申请审核通过后生成检查费用。
- 入院登记后生成住院押金或床位费用。

### 4.8 病历归档流程

流程：

```text
医生提交归档申请
  -> 病历 archiveStatus 更新为 pending
  -> 主任或管理员审核
  -> 审核通过后生成归档记录
  -> 病历 archiveStatus 更新为 archived
```

当前状态：已串通。

说明：

- 病历归档申请、审核、归档记录已经由 `emr-record-service` 管理。
- `emr-workflow-service` 支持 `record_archive` 类型，并能回写病历服务的归档审核接口。
- 未进入归档流程的病历允许删除，进入归档流程后会受状态限制。

### 4.9 内容、留言、系统配置与日志

流程：

```text
后台维护资讯/轮播/菜单/配置 -> 患者端查看资讯和留言 -> 网关写操作审计 -> 后台查看日志
```

当前状态：已串通。

说明：

- 资讯、留言、轮播图、菜单、配置、操作日志均已持久化。
- 留言会使用网关注入的可信身份写入留言人信息，避免前端伪造。
- 写操作成功后，网关会向系统服务发布操作审计。

### 4.10 AI 智能能力

流程：

```text
前端调用 AI 接口 -> AI 服务记录调用日志 -> 返回 OCR / 荐药 / 处方审核 / 智能检索结果
```

当前状态：基本串通，默认演示模式。

说明：

- AI 服务当前默认使用本地 mock provider，保证没有外部密钥也能演示。
- AI 调用会落 `ai_request_log`。
- 智能检索会优先调用病历服务查询数据，再按关键词过滤。

不足：

- PDF 中提到的外部 AI 能力如果要求真实调用，需要配置真实百度 AI Key，并完善异常、限流、脱敏策略。

## 5. 建议的期末演示路径

为了让老师能看到完整链路，建议按下面顺序演示：

1. 患者端注册或登录患者账号。
2. 患者查看科室和医生，创建预约挂号。
3. 后台端查看预约列表，确认预约记录已经产生。
4. 后台端创建病历，填写患者、医生、预约编号等信息，把预约和病历关联起来。
5. 后台端基于该患者或病历创建医嘱、处方、检查申请。
6. 后台端进入工作流，对医嘱或检查申请做审核。
7. 后台端执行审核通过的医嘱，查看执行记录。
8. 后台端创建费用，患者端查看费用并完成支付。
9. 后台端创建入院登记，再办理出院。
10. 后台端提交病历归档申请，审核通过后查看归档记录。
11. 患者端查看自己的病历、处方、检查、费用、资讯、留言。
12. 后台端查看系统日志，证明写操作有审计记录。
13. 前台或后台调用 AI OCR、荐药、处方审核、智能检索，展示智能模块。

这条路径可以覆盖 PDF 中的大部分核心功能，也能体现微服务拆分后的跨服务协作。

## 6. 当前主要缺口与补齐优先级

### P0：建议优先补强的演示闭环

1. 预约列表增加“接诊/录入病历”入口，自动带入预约、患者、医生信息。
2. 病历详情增加“开医嘱/开处方/开检查”入口，自动带入病历上下文。
3. 医嘱、检查、归档的工作流入口增加更明确的“发起审核/审核”按钮，减少手动填写业务类型和业务 ID。
4. 患者端和后台端关键新增弹窗都要检查创建成功后的列表刷新，避免出现“提示成功但页面没数据”的演示风险。

### P1：建议继续完善的 PDF 细节

1. 补患者端“忘记密码”页面入口，对接已有后端接口。
2. 补完整角色/权限管理页面，而不是只依赖 `roleCode` 和菜单配置。
3. 补处方审核工作流，或者在文档中明确处方只做 AI 审核，不走主任审核。
4. 补真实文件上传存储能力，包括大小限制、文件类型校验、下载权限和病历附件展示。
5. 补费用自动生成逻辑，让处方、检查、入院可以自动产生费用。
6. 补资讯编辑/删除等细粒度管理入口，确保和后端能力完全对应。

### P2：有时间再增强的展示能力

1. 按医生、护士、主任、管理员拆更细的首页和菜单。
2. 给数据看板补统计图表，例如预约量、病历数、费用收入、审核待办。
3. 接入真实百度 AI provider，并增加隐私脱敏、失败重试和调用限流。
4. 补更完整的端到端测试脚本，覆盖患者预约到病历归档的全链路。

## 7. 最终判断

按照两个 PDF 的要求看，当前项目已经具备期末作业所需的微服务主体结构和主要业务功能。登录注册、预约挂号、病历、医嘱、处方、检查、入院出院、费用、归档、资讯留言、系统配置、AI 等模块都能找到对应实现。

流程方面，系统可以从患者登录和预约开始，一直演示到病历录入、诊疗处理、审核、执行、费用、出院、归档和系统审计。但目前部分步骤仍是“模块之间能通过业务字段关联，但前端没有完全自动带入上下文”的状态。也就是说，流程能串起来，但还不是所有环节都做到强流程化的一键流转。

如果目标是期末演示，当前功能已经能覆盖核心要求；如果目标是更接近真实医院系统，下一步应重点补强预约到病历、病历到诊疗、诊疗到费用、归档审核这几段的自动跳转和上下文传递。
