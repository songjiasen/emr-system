# 电子病历系统功能角色流程生命周期

## 文档说明

本文按“功能模块 -> 角色 -> 操作 -> 生命周期状态 -> 数据落点”的顺序梳理当前仓库已实现和已设计的业务流程。内容以当前代码为准，参考范围包括：

- `README.md`
- `docs/api/2026-06-22-api-index.md`
- `docs/database/schema-overview.md`
- `docs/superpowers/specs/*.md`
- `backend/emr-*-service/src/main/java`
- `frontend/*/src/api` 与 `frontend/*/src/router`
- `deploy/mysql/init/*.sql`

前端统一通过网关前缀 `/cl584734139` 访问，后端服务内部接口路径不带该前缀。受保护接口使用请求头 `Token` 鉴权；网关验证成功后向下游注入 `X-User-Id`、`X-Username`、`X-Role-Code`、`X-User-Table`、`X-Department-Id`、`X-Department-Name` 等可信身份头。

## 角色总览

| 角色编码 | 中文角色 | 账号来源 | 主要职责 |
| --- | --- | --- | --- |
| `super_admin` | 超级管理员 | `emr_user.users` | 全模块维护、用户科室管理、系统配置、审计查看 |
| `admin` | 管理员 | `emr_user.users` | 全模块维护、业务兜底、系统管理 |
| `doctor` | 医生 | `emr_user.yisheng` | 预约接诊、病历录入、医嘱/处方/检查申请发起 |
| `patient` | 患者 | `emr_user.huanzhe` | 注册登录、预约挂号、查看本人病历/处方/检查/费用、留言、支付 |
| `nurse` | 护士 | `emr_user.users` | 分诊建档、入院出院、医嘱执行、按科室查看预约 |
| `director` | 主任 | `emr_user.users` | 审核任务处理、医嘱/检查/归档审核、审核记录查看 |

## 全局访问生命周期

| 生命周期阶段 | 触发操作 | 角色 | 接口 | 状态/数据变化 | 关键约束 |
| --- | --- | --- | --- | --- | --- |
| 匿名访问 | 登录、患者注册、重置密码、健康检查、医生/科室/资讯/轮播查询 | 匿名 | `/auth/login`, `/auth/patient/register`, `/auth/password/reset`, `/doctors`, `/departments`, `/news`, `/carousels` | 无登录态或创建患者账号 | 网关会清理客户端伪造身份头 |
| 登录态创建 | 登录 | 全角色 | `POST /auth/login` | 写入 `emr_auth.token`，生成有效 Token | 账号必须启用，账号、密码、角色必须匹配 |
| 登录态校验 | 访问受保护接口 | 全角色 | `POST /auth/token/validate` | 网关注入可信身份头 | Token 过期或无效时返回 401 |
| 角色鉴权 | 网关转发前校验 | 全角色 | 网关 `GatewayAccessPolicy` | 拦截越权访问 | 管理员/超级管理员拥有全模块权限，其余角色按职责最小授权 |
| 写操作审计 | 受保护写请求成功 | 登录用户 | 网关异步调用 `POST /syslogs` | 写入 `emr_system.syslog` | 排除 `/syslogs` 自身，避免递归记录 |
| 登录态结束 | 注销、改密、重置密码、Token 过期 | 全角色 | `/auth/logout`, `/auth/password/change`, `/auth/password/reset` | 删除当前或历史 Token | 改密和重置密码会清理该账号已有 Token |

## 功能角色权限矩阵

| 功能 | 患者 | 医生 | 护士 | 主任 | 管理员/超级管理员 |
| --- | --- | --- | --- | --- | --- |
| 登录注册 | 注册、登录、改密、重置、注销 | 登录、改密、重置、注销 | 登录、改密、重置、注销 | 登录、改密、重置、注销 | 登录、改密、重置、注销 |
| 用户科室 | 公开查医生/科室 | 公开查医生/科室 | 公开查医生/科室 | 公开查医生/科室 | 增删改查用户、科室 |
| 预约挂号 | 创建、查询本人、取消本人 | 查询本人接诊预约 | 查询本科室预约 | 无直接预约职责 | 全量查询、取消兜底 |
| 分诊住院 | 无直接操作 | 无直接操作 | 分诊、入院、出院、查询本人办理记录 | 无直接操作 | 全量办理和查询 |
| 病历 | 查询本人 | 创建、查询/编辑/删除本人病历，使用模板，提交归档 | 读病历，必要时新建病历 | 读病历、处理归档审核 | 全量维护 |
| 医嘱 | 无直接访问 | 创建、编辑、删除、查询本人医嘱 | 执行已通过医嘱，查医嘱 | 审核医嘱、查医嘱 | 全量维护 |
| 处方 | 查询本人处方 | 创建、编辑、取消、查询本人处方 | 无直接职责 | 查询处方 | 全量维护 |
| 检查申请 | 查询本人检查 | 创建、编辑、删除、录入结果、查询本人检查 | 无直接职责 | 审核检查、查询检查 | 全量维护 |
| 工作流 | 无直接访问 | 无直接访问 | 无直接访问 | 创建/查询/审核任务、查审核记录 | 创建/查询/审核任务、查审核记录 |
| 费用 | 查询本人、支付本人未支付费用 | 当前网关不开放费用接口 | 无直接职责 | 无直接职责 | 新增、查询、编辑、支付、删除 |
| 系统内容 | 查资讯/轮播、发表/查看本人留言、使用 AI | 留言、AI、部分内容查询 | 留言、AI | 留言、AI | 资讯、留言回复、轮播、配置、菜单、日志、AI |

## 认证与账号流程

### 生命周期

```text
患者注册 -> 账号启用(status=1) -> 登录生成Token -> Token有效 -> 改密/重置/注销/过期 -> Token失效
后台建账号 -> 账号启用(status=1) -> 登录生成Token -> 用户删除/禁用(status=0) -> 禁止继续登录
```

### 按角色操作

| 角色 | 操作 | 接口 | 数据落点 | 状态变化 | 关键限制 |
| --- | --- | --- | --- | --- | --- |
| 患者 | 注册患者账号 | `POST /auth/patient/register` | `emr_user.huanzhe` | 新增 `roleCode=patient`, `status=1` | `username`, `password`, `name` 必填，账号唯一 |
| 全角色 | 登录 | `POST /auth/login` | `emr_auth.token` | 新增 Token，有效期默认 2 小时 | 只接受启用账号；角色必须和账号来源匹配 |
| 全角色 | 校验 Token | `POST /auth/token/validate` | `emr_auth.token` | 有效则返回登录上下文，过期则删除 Token | 网关依赖此接口生成可信身份头 |
| 全角色 | 修改密码 | `POST /auth/password/change` | 账号来源表、`token` | 更新密码哈希，清理该账号历史 Token | 必须提供旧密码 |
| 全角色 | 重置密码 | `POST /auth/password/reset` | 账号来源表、`token` | 更新密码哈希，清理该账号历史 Token | 当前使用账号 + 手机号找回 |
| 全角色 | 注销 | `POST /auth/logout` | `token` | 删除当前 Token | 只影响当前令牌 |
| 管理员/超级管理员 | 新增多角色用户 | `POST /user-management/{type}` | `users`、`yisheng`、`huanzhe` | 新增账号，`status=1` | `type` 支持 `admins/doctors/patients/nurses/directors` |
| 管理员/超级管理员 | 编辑多角色用户 | `PUT /user-management/{type}/{id}` | 对应账号表 | 更新基础资料、状态等 | `roleCode/type/id` 不允许被请求体篡改 |
| 管理员/超级管理员 | 删除多角色用户 | `DELETE /user-management/{type}/{id}` | 对应账号表 | `status=0` | 软删除，禁用后不可登录 |

## 用户、科室与医生目录

### 生命周期

```text
科室创建(status=1) -> 科室编辑 -> 科室停用(status=0)
医生创建(status=1) -> 患者端公开查询 -> 后台编辑 -> 医生停用(status=0)
```

### 按角色操作

| 角色 | 操作 | 接口 | 数据落点 | 状态变化 | 关键限制 |
| --- | --- | --- | --- | --- | --- |
| 匿名/全角色 | 查询医生列表/详情 | `GET /doctors`, `GET /doctors/{id}` | `emr_user.yisheng` | 无状态变化 | 仅返回启用医生 |
| 匿名/全角色 | 查询科室列表 | `GET /departments` | `emr_user.keshileixing` | 无状态变化 | 当前列表不强制过滤 `status=1`，前端可按状态展示 |
| 管理员/超级管理员 | 新增科室 | `POST /departments` | `keshileixing` | `status=1` | `name` 必填，`sortNo` 默认 0 |
| 管理员/超级管理员 | 编辑科室 | `PUT /departments/{id}` | `keshileixing` | 可修改 `name/sortNo/status` | 科室必须存在 |
| 管理员/超级管理员 | 删除科室 | `DELETE /departments/{id}` | `keshileixing` | `status=0` | 软删除 |

## 预约挂号流程

### 生命周期

```text
pending(待处理/待接诊)
  -> cancelled(患者或管理员取消)
  -> finished(数据库预留，当前无完成接口)
  -> confirmed(数据库预留，当前无确认接口)
```

当前代码实际提供“新建、查询、取消”，尚未提供确认和完成接口。

### 按角色操作

| 角色 | 操作 | 接口 | 数据落点 | 状态变化 | 关键限制 |
| --- | --- | --- | --- | --- | --- |
| 患者 | 新增预约 | `POST /appointments` | `emr_visit.yuyueguahao` | 新增 `status=pending` | 通过网关时只能为本人预约 |
| 患者 | 查询预约列表/详情 | `GET /appointments`, `GET /appointments/{id}` | `yuyueguahao` | 无状态变化 | 通过网关时强制限定本人 `patientId` |
| 患者 | 取消预约 | `POST /appointments/{id}/cancel` | `yuyueguahao` | 非 `finished` -> `cancelled` | 通过网关时只能取消本人预约 |
| 医生 | 查询接诊预约 | `GET /appointments`, `GET /appointments/{id}` | `yuyueguahao` | 无状态变化 | 通过网关时强制限定本人 `doctorId` |
| 护士 | 查询本科室预约 | `GET /appointments` | `yuyueguahao` | 无状态变化 | 通过网关时必须有 `departmentId`，按本科室过滤 |
| 管理员/超级管理员 | 预约管理兜底 | `GET /appointments`, `GET /appointments/{id}`, `POST /appointments/{id}/cancel` | `yuyueguahao` | 可取消非完成预约 | 可全量查看和操作 |

## 分诊、入院与出院流程

### 分诊生命周期

```text
created(已建档) -> assigned(已分派，数据库预留) -> closed(已关闭，数据库预留)
```

当前代码实际只创建 `created` 并支持列表查询，尚无分派和关闭接口。

### 入出院生命周期

```text
in_hospital(住院中) -> discharged(已出院)
```

出院记录创建后，入院记录同步改为 `discharged`。

### 按角色操作

| 角色 | 操作 | 接口 | 数据落点 | 状态变化 | 关键限制 |
| --- | --- | --- | --- | --- | --- |
| 护士 | 分诊建档 | `POST /triage-records` | `emr_visit.fenzhenjiandang` | 新增 `status=created` | 通过网关时只能以本人护士身份建档 |
| 护士 | 查询分诊记录 | `GET /triage-records` | `fenzhenjiandang` | 无状态变化 | 通过网关时只能查询自己的分诊记录 |
| 护士 | 入院登记 | `POST /inpatients/admissions` | `emr_visit.ruyuanxinxi` | 新增 `status=in_hospital` | 通过网关时只能以本人护士身份办理；床位占用会拦截 |
| 护士 | 查询入院列表 | `GET /inpatients/admissions` | `ruyuanxinxi` | 无状态变化 | 当前接口支持患者、状态、病房、床位过滤 |
| 护士 | 办理出院 | `POST /inpatients/admissions/{id}/discharge` | `emr_visit.chuyuanxinxi`, `ruyuanxinxi` | 入院 `in_hospital -> discharged`，新增出院记录 | 同一入院记录不可重复出院；非住院中不可出院 |
| 护士 | 查询出院列表 | `GET /inpatients/discharges` | `chuyuanxinxi` | 无状态变化 | 支持按患者或入院记录过滤 |
| 管理员/超级管理员 | 分诊住院兜底 | 同上 | 同上 | 同上 | 可全量操作，不受本人护士 ID 限制 |

## 病历、模板与归档流程

### 病历生命周期

```text
not_submitted(未提交归档)
  -> pending(归档待审核)
  -> archived(已归档，不允许修改/删除)
  -> rejected(归档驳回，不能删除，可再次提交时需先满足业务调整)
```

### 模板生命周期

```text
status=1(启用) -> status=0(停用/删除)
```

### 归档申请生命周期

```text
pending(待审核) -> approved(审核通过并生成归档快照)
pending(待审核) -> rejected(审核驳回)
```

### 按角色操作

| 角色 | 操作 | 接口 | 数据落点 | 状态变化 | 关键限制 |
| --- | --- | --- | --- | --- | --- |
| 患者 | 查询本人病历 | `GET /medical-records`, `GET /medical-records/{id}` | `emr_record.binglixinxi` | 无状态变化 | 通过网关时强制限定本人 `patientId` |
| 医生 | 新增病历 | `POST /medical-records` | `binglixinxi` | 新增 `archiveStatus=not_submitted` | 通过网关时只能以本人 `doctorId` 创建 |
| 医生 | 查询本人病历 | `GET /medical-records`, `GET /medical-records/{id}` | `binglixinxi` | 无状态变化 | 通过网关时强制限定本人 `doctorId` |
| 医生 | 编辑病历 | `PUT /medical-records/{id}` | `binglixinxi` | 正文和关联字段更新 | 已归档病历禁止修改；医生只能维护本人病历 |
| 医生 | 删除病历 | `DELETE /medical-records/{id}` | `binglixinxi` | 物理删除 | 只有 `not_submitted` 可删；医生只能删本人病历 |
| 医生/管理员 | 上传病历附件演示 | `POST /medical-records/upload` | 当前返回演示路径 | 无状态变化 | 当前未真正接 Multipart 文件落盘 |
| 医生/管理员 | 新增模板 | `POST /medical-record-templates` | `emr_record.binglimoban` | 新增 `status=1` | 模板名和内容必填 |
| 医生/管理员 | 查询模板 | `GET /medical-record-templates`, `GET /medical-record-templates/{id}` | `binglimoban` | 无状态变化 | 当前列表返回全部模板，可由前端过滤启用状态 |
| 医生/管理员 | 编辑模板 | `PUT /medical-record-templates/{id}` | `binglimoban` | 更新模板字段或 `status` | 模板必须存在 |
| 医生/管理员 | 删除模板 | `DELETE /medical-record-templates/{id}` | `binglimoban` | `status=0` | 软删除 |
| 医生/管理员 | 提交归档申请 | `POST /medical-record-archives/applications` | `bingliguidangshenqing`, `binglixinxi` | 申请 `pending`，病历 `archiveStatus=pending` | 病历已 `pending/archived` 时不可重复提交 |
| 主任/管理员 | 查询归档申请 | `GET /medical-record-archives/applications` | `bingliguidangshenqing` | 无状态变化 | 当前接口未按角色自动过滤 |
| 主任/管理员 | 审核归档申请 | `POST /medical-record-archives/applications/{id}/audit` | `bingliguidangshenqing`, `bingliguidang`, `binglixinxi` | `approved` 时生成归档快照并把病历置为 `archived`；`rejected` 时病历置为 `rejected` | 审核结果只允许 `approved/rejected` |
| 全授权后台角色 | 查询归档记录 | `GET /medical-record-archives` | `bingliguidang` | 无状态变化 | 归档记录是审核通过后的病历快照 |

## 诊疗流程：医嘱、处方、检查

### 医嘱生命周期

```text
pending_audit(医生新建待审核)
  -> approved(主任/管理员审核通过)
  -> executed(护士/管理员执行)

pending_audit -> rejected(审核驳回)
rejected -> pending_audit(医生修改后重新待审核，取决于编辑内容)
approved -> pending_audit(已通过医嘱修改内容后重新待审核)
```

### 处方生命周期

```text
created(已开具) -> cancelled(已取消)
```

### 检查申请生命周期

```text
pending_audit(待审核)
  -> approved(审核通过)
  -> finished(录入检查结果后完成)

pending_audit -> rejected(审核驳回)
```

### 按角色操作

| 角色 | 操作 | 接口 | 数据落点 | 状态变化 | 关键限制 |
| --- | --- | --- | --- | --- | --- |
| 医生 | 新增医嘱 | `POST /medical-orders` | `emr_clinical.yizhuxinxi` | 新增 `status=pending_audit` | 医生通过网关时只能以本人身份录入 |
| 医生 | 查询本人医嘱 | `GET /medical-orders`, `GET /medical-orders/{id}` | `yizhuxinxi` | 无状态变化 | 通过网关时强制限定本人 `doctorId` |
| 医生 | 编辑医嘱 | `PUT /medical-orders/{id}` | `yizhuxinxi` | 已通过医嘱改内容会回到 `pending_audit` | `executed` 禁止修改 |
| 医生 | 删除医嘱 | `DELETE /medical-orders/{id}` | `yizhuxinxi` | 未执行医嘱物理删除 | `executed` 禁止删除 |
| 主任/管理员 | 审核医嘱 | `POST /medical-orders/{id}/audit-result` 或工作流回写 | `yizhuxinxi` | `pending_audit/rejected/approved -> approved/rejected` | 业务服务允许主任/管理员；当前网关主任建议走工作流任务审核 |
| 护士/管理员 | 执行医嘱 | `POST /medical-orders/{id}/execute` | `yizhuzhixingjilu`, `yizhuxinxi` | 医嘱 `approved -> executed`，新增执行记录 | 未审核通过不能执行；护士只能以本人身份执行 |
| 护士/管理员 | 查询执行记录 | `GET /medical-orders/{id}/executions` | `yizhuzhixingjilu` | 无状态变化 | 需传医嘱 ID |
| 医生 | 新增处方 | `POST /prescriptions` | `emr_clinical.kaifang` | 新增 `status=created` | 药品名称、患者、医生必填；医生只能以本人身份开具 |
| 患者 | 查询本人处方 | `GET /prescriptions`, `GET /prescriptions/{id}` | `kaifang` | 无状态变化 | 通过网关时强制限定本人 `patientId` |
| 医生 | 查询/编辑/取消本人处方 | `GET/PUT/DELETE /prescriptions...` | `kaifang` | 删除接口将 `created -> cancelled` | `cancelled` 不允许修改；医生只能维护本人处方 |
| 主任 | 查询处方 | `GET /prescriptions` | `kaifang` | 无状态变化 | 网关只开放主任查询 |
| 医生 | 新增检查申请 | `POST /test-requests` | `emr_clinical.jianchaxiang` | 新增 `status=pending_audit` | 检查项目必填；医生只能以本人身份提交 |
| 患者 | 查询本人检查 | `GET /test-requests`, `GET /test-requests/{id}` | `jianchaxiang` | 无状态变化 | 通过网关时强制限定本人 `patientId` |
| 医生 | 查询/编辑/删除本人检查申请 | `GET/PUT/DELETE /test-requests...` | `jianchaxiang` | 录入 `resultContent` 且当前为 `approved` 时变为 `finished` | `finished` 禁止修改和删除 |
| 主任/管理员 | 审核检查申请 | `POST /test-requests/{id}/audit-result` 或工作流回写 | `jianchaxiang` | `pending_audit -> approved/rejected` | `finished` 禁止再次审核；当前网关主任建议走工作流任务审核 |

## 工作流审核中心

### 生命周期

```text
pending(待审核任务)
  -> approved(审核通过，已回写业务服务)
  -> rejected(审核驳回，已回写业务服务)
```

### 支持业务类型

| `businessType` | 对应业务 | 回写接口 |
| --- | --- | --- |
| `medical_order` | 医嘱审核 | `POST /medical-orders/{id}/audit-result` |
| `test_request` | 检查申请审核 | `POST /test-requests/{id}/audit-result` |
| `record_archive` | 病历归档审核 | `POST /medical-record-archives/applications/{id}/audit` |
| `medical_record` | 病历质量审核扩展 | 当前作为扩展类型保留 |

### 按角色操作

| 角色 | 操作 | 接口 | 数据落点 | 状态变化 | 关键限制 |
| --- | --- | --- | --- | --- | --- |
| 主任/管理员 | 创建审核任务 | `POST /workflow/tasks` | `emr_workflow.shenherenwu` | 新增 `status=pending` | 同一 `businessType + businessId` 不能重复存在 pending 任务 |
| 主任/管理员 | 查询审核任务 | `GET /workflow/tasks` | `shenherenwu` | 无状态变化 | 支持按业务类型、业务 ID、状态、审核角色、申请人过滤 |
| 主任/管理员 | 审核任务 | `POST /workflow/tasks/{id}/audit` | `shenherenwu`, `shenhejilu`, 业务服务表 | 业务回写成功后，任务 `pending -> approved/rejected` 并新增审核记录 | 只允许审核 pending 任务；回写失败返回 502，不静默成功 |
| 主任/管理员 | 查询审核记录 | `GET /workflow/audit-records` | `emr_workflow.shenhejilu` | 无状态变化 | 支持按任务、审核人、审核结果过滤 |

## 费用与支付流程

### 生命周期

```text
unpaid(未支付)
  -> paid(已支付)
  -> refunded(数据库预留，当前无退费接口)
```

### 按角色操作

| 角色 | 操作 | 接口 | 数据落点 | 状态变化 | 关键限制 |
| --- | --- | --- | --- | --- | --- |
| 管理员/超级管理员 | 新增费用 | `POST /fees` | `emr_billing.feiyong` | 新增 `payStatus=unpaid` | `amount` 不能为负数；`businessType` 默认 `manual` |
| 管理员/超级管理员 | 查询费用 | `GET /fees`, `GET /fees/{id}` | `feiyong` | 无状态变化 | 支持按患者、支付状态、业务来源过滤 |
| 管理员/超级管理员 | 编辑费用 | `PUT /fees/{id}` | `feiyong` | 未支付费用字段更新 | `paid/refunded` 禁止修改 |
| 管理员/超级管理员 | 删除费用 | `DELETE /fees/{id}` | `feiyong` | 未支付费用物理删除 | `paid/refunded` 禁止删除 |
| 管理员/超级管理员 | 代支付费用 | `POST /fees/{id}/pay` | `feiyong` | `unpaid -> paid`，写入 `payTime` | 已支付或退费不可重复支付 |
| 患者 | 查询本人费用 | `GET /fees`, `GET /fees/{id}` | `feiyong` | 无状态变化 | 通过网关时强制限定本人 `patientId` |
| 患者 | 支付本人费用 | `POST /fees/{id}/pay` | `feiyong` | `unpaid -> paid` | 只能支付本人费用 |

当前网关未给医生、护士、主任开放费用接口。虽然部分前端 API 文件存在 `createFee` 等函数，但以网关鉴权为准，患者端只能查询和支付本人费用。

## 系统内容、留言、菜单与日志

### 资讯生命周期

```text
status=1(发布/有效) -> status=0(下架/删除)
publishStatus 默认为 published，可按展示口径扩展
```

### 留言生命周期

```text
status=1(正常/待回复展示为 pending) -> 已回复(写 replyContent/replyTime) -> status=0(删除)
```

### 轮播生命周期

```text
status=1(启用) -> status=0(停用/删除)
```

### 按角色操作

| 角色 | 操作 | 接口 | 数据落点 | 状态变化 | 关键限制 |
| --- | --- | --- | --- | --- | --- |
| 匿名/全角色 | 查询资讯 | `GET /news`, `GET /news/{id}` | `emr_system.news` | 无状态变化 | 公开查询 |
| 管理员/超级管理员 | 新增资讯 | `POST /news` | `news` | 新增 `status=1`, `publishStatus=published` | 标题和内容必填 |
| 管理员/超级管理员 | 编辑资讯 | `PUT /news/{id}` | `news` | 更新内容或状态 | 资讯必须存在 |
| 管理员/超级管理员 | 删除资讯 | `DELETE /news/{id}` | `news` | `status=0` | 软删除 |
| 全登录角色 | 发表留言 | `POST /messages` | `emr_system.messages` | 新增 `status=1` | 登录时使用可信身份头写用户信息；匿名请求可传用户信息 |
| 患者 | 查询本人留言 | `GET /messages` | `messages` | 无状态变化 | 患者通过网关时只返回本人留言 |
| 非患者角色 | 查询留言 | `GET /messages` | `messages` | 无状态变化 | 当前服务端不额外限制医生/护士/主任查询范围 |
| 非患者角色 | 回复留言 | `POST /messages/{id}/reply` | `messages` | 写入回复内容、回复人、回复时间 | 服务端明确禁止患者回复留言 |
| 管理员/超级管理员 | 删除留言 | `DELETE /messages/{id}` | `messages` | `status=0` | 当前网关对 `/messages` 较宽，实际产品口径建议由后台角色操作 |
| 匿名/全角色 | 查询轮播 | `GET /carousels` | `emr_system.carousels` | 无状态变化 | 公开查询 |
| 管理员/超级管理员 | 新增/编辑/删除轮播 | `POST /carousels`, `PUT /carousels/{id}`, `DELETE /carousels/{id}` | `carousels` | 新增 `status=1`，删除置 `status=0` | 标题和图片必填 |
| 管理员/超级管理员 | 保存配置 | `POST /config` | `emr_system.config` | 不存在则新增，存在则覆盖 | `configKey` 必填 |
| 管理员/超级管理员 | 查询配置 | `GET /config/{key}` | `config` | 无状态变化 | 配置不存在返回 400 |
| 管理员/超级管理员 | 保存菜单 | `POST /menus` | `emr_system.menu` | 按 `roleCode` 新增或覆盖菜单 JSON | `roleCode` 必填 |
| 管理员/超级管理员 | 查询菜单 | `GET /menus/{roleCode}` | `menu` | 无状态变化 | 菜单不存在返回 400 |
| 管理员/超级管理员 | 查询审计日志 | `GET /syslogs` | `emr_system.syslog` | 无状态变化 | 网关成功写操作会自动写日志 |

## AI 辅助流程

### AI 调用日志生命周期

```text
调用开始 -> 参数校验/外部服务调用 -> success(成功日志) 或 failed(失败日志)
```

### 按角色操作

| 角色 | 操作 | 接口 | 数据落点 | 状态变化 | 关键限制 |
| --- | --- | --- | --- | --- | --- |
| 全登录角色 | OCR 病历识别 | `POST /ai/ocr` | `emr_ai.ai_request_log` | 成功写 `status=success`，失败写 `failed` | `fileUrl` 和 `imageBase64` 至少传一个 |
| 全登录角色 | 智能荐药 | `POST /ai/recommend-medicine` | `ai_request_log` | 写调用日志 | `diagnosis` 必填；mock provider 给演示建议 |
| 全登录角色 | 处方审核 | `POST /ai/prescription-audit` | `ai_request_log` | 写调用日志 | 空处方、重复药品、禁忌关键词会产生警告 |
| 全登录角色 | 智能检索 | `POST /ai/smart-search` | `ai_request_log`，读取 `emr-record-service` | 病历服务可用时 success，不可用时 failed 但返回空结果 | `keyword` 必填；支持 `patientId/doctorId` 缩小范围 |

AI 服务默认使用 `mock` provider，不依赖外部百度 AI Key。日志只保存输入/输出摘要，不保存完整长篇隐私文本。

## 前端入口对应关系

### 患者端 `frontend/emr-frontend`

| 路由 | 对应功能 | 主要 API |
| --- | --- | --- |
| `/home` | 首页、公开内容入口 | `/news`, `/carousels`, `/doctors` |
| `/doctors` | 医生列表/详情 | `/doctors`, `/doctors/{id}` |
| `/appointment` | 预约挂号、我的预约、取消预约 | `/appointments`, `/appointments/{id}/cancel` |
| `/record` | 我的病历 | `/medical-records`, `/medical-records/{id}` |
| `/fees` | 我的费用、支付 | `/fees`, `/fees/{id}/pay` |
| `/prescriptions` | 我的处方 | `/prescriptions` |
| `/tests` | 我的检查申请 | `/test-requests` |
| `/news` | 资讯列表/详情 | `/news` |
| `/messages` | 在线留言 | `/messages` |
| `/profile` | 个人中心、改密、注销 | `/auth/password/change`, `/auth/logout` |
| `/ai` | AI 智能检索/辅助 | `/ai/*` |

### 后台端 `frontend/manage_code`

| 路由 | 对应功能 | 主要 API |
| --- | --- | --- |
| `/dashboard` | 管理端仪表盘 | 汇总入口 |
| `/appointments` | 预约管理 | `/appointments` |
| `/departments` | 科室管理 | `/departments` |
| `/managed-users` | 多角色用户管理 | `/user-management/{type}` |
| `/records` | 病历管理 | `/medical-records` |
| `/triage` | 分诊建档 | `/triage-records` |
| `/admissions` | 入院登记 | `/inpatients/admissions` |
| `/discharges` | 出院办理/出院列表 | `/inpatients/discharges` |
| `/templates` | 病历模板 | `/medical-record-templates` |
| `/orders` | 医嘱管理 | `/medical-orders` |
| `/prescriptions` | 处方管理 | `/prescriptions` |
| `/tests` | 检查申请 | `/test-requests` |
| `/workflow-tasks` | 审核任务 | `/workflow/tasks` |
| `/workflow-audits` | 审核记录 | `/workflow/audit-records` |
| `/archive-applications` | 归档申请 | `/medical-record-archives/applications` |
| `/archives` | 归档记录 | `/medical-record-archives` |
| `/billing` | 费用管理 | `/fees` |
| `/news` | 资讯管理 | `/news` |
| `/carousels` | 轮播管理 | `/carousels` |
| `/messages` | 留言管理 | `/messages` |
| `/syslogs` | 操作日志 | `/syslogs` |
| `/config` | 系统配置 | `/config` |
| `/menus` | 菜单配置 | `/menus` |
| `/ai` | AI 工具 | `/ai/*` |

当前两个前端路由已建立入口，源码中的 `views` 多数仍是占位目录或空壳页面；生命周期以 API 和后端服务实现为准。

## 主演示闭环

```text
患者注册/登录
  -> 查询医生/科室
  -> 创建预约(pending)
  -> 医生查询本人预约
  -> 医生创建病历(not_submitted)
  -> 医生创建医嘱(pending_audit)、处方(created)、检查申请(pending_audit)
  -> 主任/管理员通过工作流或回写接口审核医嘱/检查(approved/rejected)
  -> 护士执行已通过医嘱(executed)
  -> 管理员录入费用(unpaid)
  -> 患者查询并支付费用(paid)
  -> 医生提交病历归档申请(pending)
  -> 主任/管理员审核归档(archived/rejected)
  -> 管理员查看审计日志
```

## 当前实现边界

| 模块 | 已落地 | 当前边界 |
| --- | --- | --- |
| 预约 | 创建、查询、取消 | `confirmed/finished` 状态已在数据库预留，但当前没有确认/完成接口 |
| 分诊 | 创建、查询 | `assigned/closed` 状态已预留，但当前没有分派/关闭接口 |
| 入出院 | 入院、出院、出院列表 | 出院会同步入院状态；病房床位只按 `wardNo + bedNo + in_hospital` 拦截 |
| 病历 | 创建、查询、编辑、删除、上传演示 | 上传接口当前只返回演示文件路径；已归档病历禁止修改 |
| 模板 | 增删改查 | 删除为 `status=0`，列表当前返回全部模板 |
| 归档 | 申请、审核、归档列表 | 归档审核可在记录服务内闭环，也可由工作流服务回写 |
| 医嘱 | 创建、审核、执行、执行记录 | 主任经网关直接访问 `/audit-result` 可能不在白名单，推荐走工作流审核 |
| 处方 | 创建、查询、编辑、取消 | 删除接口实际是状态取消，不物理删除 |
| 检查 | 创建、审核、录结果、完成 | `finished` 后禁止修改、删除、审核 |
| 工作流 | 任务、审核、记录、业务回写 | 回写失败会返回 502，避免任务成功但业务状态未同步 |
| 费用 | 新增、查询、编辑、支付、删除 | 只有 `unpaid` 可改删；`refunded` 状态预留，无退费接口 |
| 系统内容 | 资讯、留言、轮播、配置、菜单、日志 | 留言接口网关白名单较宽，产品口径建议后台角色处理删除和回复 |
| AI | OCR、荐药、处方审核、智能检索、日志 | 默认 mock provider；智能检索依赖病历服务可用性 |
