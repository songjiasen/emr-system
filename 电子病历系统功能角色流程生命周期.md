# 电子病历系统角色接力流程生命周期

## 文档说明

本文不按功能模块罗列权限，而是按“患者发起后，谁接着处理、处理完交给谁、最终怎么结束”的方式梳理电子病历系统主流程。重点回答三个问题：

- 当前节点由哪个角色处理。
- 这个角色具体做什么。
- 做完之后下一步流向哪里。

当前项目统一通过网关前缀 `/cl584734139` 访问后端能力。受保护接口使用 `Token` 鉴权，网关校验后向下游注入 `X-User-Id`、`X-Role-Code`、`X-Department-Id` 等可信身份头，各服务再按角色限制本人、本医生、本科室或全量范围。

## 一句话主流程

```text
患者预约挂号
  -> 医生查看预约并接诊
  -> 医生录入病历
  -> 医生开医嘱、处方、检查申请
  -> 主任审核需要审核的医嘱和检查
  -> 护士执行审核通过的医嘱
  -> 医生或工作人员录入检查结果
  -> 管理员生成费用
  -> 患者查看并支付费用
  -> 医生提交病历归档
  -> 主任审核归档
  -> 病历归档完成
```

## 参与角色和各自接力位置

| 角色 | 什么时候上场 | 主要动作 | 处理完交给谁 |
| --- | --- | --- | --- |
| 患者 | 流程开始、费用支付、结果查看 | 注册登录、选择医生和时间预约、查看病历/处方/检查/费用、支付费用 | 预约后交给医生；支付后流程进入收尾 |
| 医生 | 患者预约后 | 查看本人预约、接诊、录入病历、开医嘱、开处方、开检查、提交归档 | 医嘱/检查交给主任审核；病历归档交给主任审核 |
| 主任 | 有审核事项时 | 审核医嘱、审核检查申请、审核病历归档 | 医嘱通过后交给护士执行；检查通过后交给医生/工作人员录结果；归档通过后流程结束 |
| 护士 | 医嘱审核通过或需要住院护理时 | 执行医嘱、记录执行情况；可做分诊、入院、出院 | 执行后交回医生/管理员继续诊疗、收费或归档 |
| 管理员/超级管理员 | 需要兜底和收费时 | 维护用户科室、查看全量业务、录入或处理费用、查看日志 | 费用生成后交给患者支付 |

## 主流程：从预约到归档结束

### 1. 患者发起预约

```text
患者登录
  -> 查看医生和科室
  -> 选择医生、日期、时间段
  -> 提交预约
  -> 系统生成预约记录 pending
```

| 谁 | 做什么 | 系统记录 | 下一步 |
| --- | --- | --- | --- |
| 患者 | 在患者端选择医生、科室、预约时间并提交预约 | 写入 `emr_visit.yuyueguahao`，状态为 `pending` | 医生在后台查看自己的接诊预约 |
| 患者 | 如果不去就诊，可以取消本人预约 | 预约状态从非完成状态改为 `cancelled` | 流程提前结束 |
| 管理员 | 必要时替患者兜底查询或取消预约 | 同样更新预约状态 | 取消后流程结束；未取消则继续等待接诊 |

当前代码已经支持预约创建、查询和取消。数据库预留了 `confirmed`、`finished` 状态，但目前没有单独的“确认预约”或“完成预约”接口，所以“处理预约”的实际动作是医生查看预约后继续创建病历。

### 2. 医生接收预约并开始接诊

```text
预约 pending
  -> 医生查看本人预约
  -> 医生确认患者信息
  -> 医生开始录入病历
```

| 谁 | 做什么 | 系统记录 | 下一步 |
| --- | --- | --- | --- |
| 医生 | 在预约列表查看分配给自己的预约 | 读取 `yuyueguahao`，网关限制医生只能看本人 `doctorId` 的预约 | 医生创建病历 |
| 护士 | 可以按本科室查看预约，辅助线下分诊或提醒 | 读取本科室预约，网关按 `departmentId` 过滤 | 需要分诊时进入分诊流程；否则继续医生接诊 |
| 管理员 | 可查看全部预约，处理演示或异常数据 | 读取全量预约 | 继续医生接诊或取消异常预约 |

这里没有新的预约状态变更。预约只是接诊入口，真正进入诊疗流程的标志是医生创建病历。

### 3. 医生录入病历

```text
医生接诊
  -> 新建病历
  -> 填写主诉、现病史、诊断、治疗建议
  -> 病历状态 not_submitted
```

| 谁 | 做什么 | 系统记录 | 下一步 |
| --- | --- | --- | --- |
| 医生 | 根据本次接诊录入病历 | 写入 `emr_record.binglixinxi`，归档状态为 `not_submitted` | 医生继续开医嘱、处方或检查 |
| 医生 | 可编辑未归档病历 | 更新病历正文、诊断、建议等字段 | 继续诊疗或提交归档 |
| 患者 | 查看自己的病历 | 只能读取本人 `patientId` 的病历 | 等待后续处方、检查、费用等结果 |
| 主任 | 只读复核病历 | 只能查看，不直接新增、编辑、删除病历 | 后续处理归档审核 |

病历是后续医嘱、处方、检查、费用和归档的核心业务对象。只要病历已经归档，就不允许继续修改或删除。

### 4. 医生开医嘱、处方、检查

```text
病历已创建
  -> 医生按诊疗需要开医嘱
  -> 医生按诊疗需要开处方
  -> 医生按诊疗需要提交检查申请
```

| 业务 | 谁处理 | 做什么 | 状态变化 | 下一步 |
| --- | --- | --- | --- | --- |
| 医嘱 | 医生 | 创建医嘱，例如用药、护理、治疗安排 | 新增 `pending_audit` | 交给主任审核 |
| 处方 | 医生 | 开具药品处方 | 新增 `created` | 患者可查看；需要收费时交给管理员生成费用 |
| 检查申请 | 医生 | 提交检查项目申请 | 新增 `pending_audit` | 交给主任审核 |

处方当前不进入工作流审核，医生开具后患者可以查看。医嘱和检查申请需要审核，通过后才能进入执行或结果维护。

### 5. 主任审核医嘱和检查

```text
医嘱 pending_audit / 检查 pending_audit
  -> 主任查看审核任务
  -> 主任审核通过或驳回
  -> 系统回写业务状态
```

| 谁 | 做什么 | 系统记录 | 下一步 |
| --- | --- | --- | --- |
| 主任 | 审核医生提交的医嘱 | 医嘱 `pending_audit -> approved` 或 `rejected` | 通过后交给护士执行；驳回后退回医生修改 |
| 主任 | 审核医生提交的检查申请 | 检查 `pending_audit -> approved` 或 `rejected` | 通过后交给医生或工作人员录入检查结果；驳回后退回医生修改 |
| 管理员 | 可作为兜底角色处理审核 | 同主任审核结果 | 继续对应后续流程 |

推荐演示主链路走工作流任务，因为工作流会保存审核任务和审核记录；也可以通过业务服务的审核回写接口直接修改状态。

### 6. 护士执行已审核医嘱

```text
医嘱 approved
  -> 护士查看可执行医嘱
  -> 护士执行医嘱
  -> 生成执行记录
  -> 医嘱 executed
```

| 谁 | 做什么 | 系统记录 | 下一步 |
| --- | --- | --- | --- |
| 护士 | 执行主任审核通过的医嘱 | 写入 `emr_clinical.yizhuzhixingjilu`，医嘱状态改为 `executed` | 医生继续观察诊疗结果，管理员可生成费用 |
| 管理员 | 可兜底执行医嘱 | 同样生成执行记录 | 继续收费或归档 |

未审核通过的医嘱不能执行。已经执行的医嘱不能再编辑或删除。

### 7. 检查结果维护

```text
检查申请 approved
  -> 医生或工作人员录入检查结果
  -> 检查状态 finished
  -> 患者查看检查结果
```

| 谁 | 做什么 | 系统记录 | 下一步 |
| --- | --- | --- | --- |
| 医生 | 录入检查结果内容 | 检查申请从 `approved` 更新为 `finished` | 患者可查看结果；管理员可生成费用 |
| 患者 | 查看本人检查申请和结果 | 只能查询本人检查 | 等待费用支付或后续诊疗 |

检查完成后禁止继续修改、删除或重复审核。

### 8. 管理员生成费用

```text
处方 / 检查 / 医嘱执行 / 住院等业务完成
  -> 管理员新增费用
  -> 费用 unpaid
  -> 患者端出现待支付费用
```

| 谁 | 做什么 | 系统记录 | 下一步 |
| --- | --- | --- | --- |
| 管理员/超级管理员 | 根据处方、检查、治疗或住院情况新增费用 | 写入 `emr_billing.feiyong`，`payStatus=unpaid` | 患者查看并支付 |
| 管理员/超级管理员 | 可编辑或删除未支付费用 | 未支付费用可改删 | 改完继续等待患者支付 |

当前费用不会由处方、检查或住院流程自动生成，需要管理员手工新增。费用可以保存业务来源类型和业务 ID，用来关联处方、检查、住院等来源。

### 9. 患者支付费用

```text
费用 unpaid
  -> 患者查看本人费用
  -> 患者支付
  -> 费用 paid
```

| 谁 | 做什么 | 系统记录 | 下一步 |
| --- | --- | --- | --- |
| 患者 | 查看本人待支付费用 | 只能查询本人 `patientId` 的费用 | 确认后支付 |
| 患者 | 支付本人费用 | 费用 `unpaid -> paid`，写入 `payTime` | 门诊费用流程结束，进入病历归档收尾 |
| 管理员 | 可代支付费用 | 同样更新为 `paid` | 继续归档或查看日志 |

已支付费用不能重复支付，也不能继续编辑或删除。数据库预留 `refunded`，当前没有退费接口。

### 10. 医生提交病历归档

```text
诊疗和收费结束
  -> 医生提交病历归档申请
  -> 归档申请 pending
  -> 病历 archiveStatus=pending
```

| 谁 | 做什么 | 系统记录 | 下一步 |
| --- | --- | --- | --- |
| 医生 | 对完成诊疗的病历提交归档申请 | 写入 `bingliguidangshenqing`，病历状态改为 `pending` | 交给主任审核 |
| 管理员 | 可兜底提交归档申请 | 同医生提交结果 | 交给主任或管理员审核 |

同一病历已处于 `pending` 或 `archived` 时不能重复提交归档。

### 11. 主任审核归档并结束流程

```text
归档申请 pending
  -> 主任审核
  -> 通过：生成归档快照，病历 archived
  -> 驳回：病历 rejected，退回医生处理
```

| 谁 | 做什么 | 系统记录 | 流程结果 |
| --- | --- | --- | --- |
| 主任 | 审核病历归档申请 | 通过时写入 `bingliguidang`，病历 `archived` | 病历完成归档，流程结束 |
| 主任 | 驳回归档申请 | 申请 `rejected`，病历 `archiveStatus=rejected` | 退回医生补充或调整 |
| 管理员 | 可兜底审核归档 | 同主任审核结果 | 通过后结束，驳回后退回 |

病历归档通过后，归档记录保存的是病历快照。已归档病历不能再修改或删除。

## 可选分支：护士分诊、入院、出院

这条分支适用于需要住院或护理建档的场景，不是每个门诊预约都必须走。

```text
患者到院
  -> 护士分诊建档
  -> 护士办理入院
  -> 医生诊疗、开医嘱
  -> 主任审核医嘱
  -> 护士执行医嘱
  -> 护士办理出院
  -> 管理员生成住院费用
  -> 患者支付
  -> 病历归档
```

| 节点 | 谁处理 | 做什么 | 状态或结果 |
| --- | --- | --- | --- |
| 分诊 | 护士 | 创建分诊记录，登记患者、护士、主诉、分诊级别 | `fenzhenjiandang.status=created` |
| 入院 | 护士 | 办理入院，分配病房和床位 | `ruyuanxinxi.status=in_hospital` |
| 医嘱执行 | 护士 | 执行已审核通过的医嘱 | 医嘱 `approved -> executed` |
| 出院 | 护士 | 基于入院记录办理出院 | 入院记录 `in_hospital -> discharged`，新增出院记录 |
| 收费 | 管理员 | 录入住院相关费用 | 费用 `unpaid`，等待患者支付 |

床位占用会按病房号和床位号拦截，同一入院记录不能重复出院。

## 异常和提前结束场景

| 场景 | 谁处理 | 结果 |
| --- | --- | --- |
| 患者预约后取消 | 患者或管理员 | 预约改为 `cancelled`，流程结束 |
| 主任驳回医嘱 | 主任 | 医嘱改为 `rejected`，医生需要修改后重新提交 |
| 主任驳回检查申请 | 主任 | 检查改为 `rejected`，医生需要修改后重新提交 |
| 医嘱已执行 | 护士执行后形成 | 医嘱不可再编辑或删除 |
| 检查已完成 | 医生录入结果后形成 | 检查不可再编辑、删除或审核 |
| 费用已支付 | 患者或管理员支付后形成 | 费用不可再编辑、删除或重复支付 |
| 归档被驳回 | 主任或管理员 | 病历进入 `rejected`，医生需要补充后再处理 |
| 病历已归档 | 主任审核通过后形成 | 病历不可修改或删除，流程正式结束 |

## 当前实现边界

| 环节 | 当前已经做到 | 当前没有做到 |
| --- | --- | --- |
| 预约 | 患者创建、患者查询本人、医生查本人、护士查本科室、管理员全量、取消预约 | 没有确认预约和完成预约接口 |
| 接诊 | 医生可以通过预约信息继续创建病历 | 没有单独的“接诊按钮”自动把预约状态改为已接诊 |
| 病历 | 医生创建、编辑、删除未归档病历，患者查看本人病历，主任只读复核 | 已归档病历不能改；附件上传偏演示路径 |
| 医嘱 | 医生创建，主任审核，护士执行 | 未审核通过不能执行；已执行不能改删 |
| 处方 | 医生开具，患者查看，医生可取消 | 当前不走主任审核 |
| 检查 | 医生申请，主任审核，医生录结果，患者查看 | 检查完成后不能再改删 |
| 费用 | 管理员手工新增，患者支付本人费用 | 不会从处方、检查、住院自动生成费用 |
| 归档 | 医生申请，主任审核，通过后生成归档快照 | 归档后不能修改病历 |
| 分诊住院 | 护士分诊、入院、出院 | 分诊的分派和关闭状态预留，当前没有独立接口 |

## 演示时可以按这个顺序操作

```text
1. 患者登录患者端，预约一个医生。
2. 医生登录后台，查看自己的预约。
3. 医生根据该患者录入病历。
4. 医生在病历基础上开医嘱、开处方、提交检查申请。
5. 主任登录后台，审核医嘱和检查申请。
6. 护士登录后台，执行已审核通过的医嘱。
7. 医生录入检查结果。
8. 管理员登录后台，为患者新增费用。
9. 患者回到患者端，查看费用并支付。
10. 医生提交病历归档申请。
11. 主任审核归档，通过后病历归档完成。
```

如果需要演示住院流程，可以在第 2 步后增加“护士分诊建档、护士入院登记”，在第 8 步前增加“护士办理出院”，再由管理员录入住院费用。
